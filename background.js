// background.js - minimal service worker for Bottle Stop Craft Beer Link Collector
self.addEventListener('install', () => {
  // Extension installed
});

self.addEventListener('activate', () => {
  // Extension activated
}); 